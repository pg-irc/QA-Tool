// tslint:disable:quotemark trailing-comma

export const objectWithToken = {
    "type": "object",
    "properties": {
        "token": {
            "type": "string",
        }
    },
    "required": ["token"]
};
